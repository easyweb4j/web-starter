logging.level:
  root: INFO
  org.springframework.web: INFO

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/dev?useUnicode=true&useSSL=false&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    hikari:
      idleTimeout: 300000
      connectionTimeout: 10000
      username: dev
      password: "dev"
      maximumPoolSize: 3
      transactionIsolation: TRANSACTION_READ_COMMITTED

api:
  post:
    url: "https://jsonplaceholder.typicode.com/posts/"

